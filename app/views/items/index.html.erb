<h1>Items</h1>
<p><%= link_to 'New Item', new_item_path %></p>

<% @items.each do |item| %>
	<h2><%= item.title %></h2>
	<p><%= item.item_type.name %></p>
	<p><%= item.completed? %></p>
	<%= image_tag(item.image_url) %>
<% end %>